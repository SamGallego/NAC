<div class="container">
    <h1>{{event.name}}</h1>
    <section>{{event.movie.name}}</section>
    <p>{{event.startDate}}</p>
    {{#if canJoin}}
    <button>I am the REAL Join :D</button>
    {{/if}}
    <hr>
    <section> {{comment.comment}}</section>
</div>